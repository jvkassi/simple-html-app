server {
    listen 8080;
    server_tokens off;

    location / {
        root /usr/share/nginx/html;
        index index.html;
    }

    location = /healthz {
        return 200 "ok\n";
        add_header Content-Type text/plain;
    }
}
